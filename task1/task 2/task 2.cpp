

#include <iostream>
#include "math.h"

using namespace std;




int main()
{
	int a = 4;
	int b = 8;
	int c = 6;
	double p = (a + b + c) / 2;
	double S = sqrt(p * (p - a) * (p - b) * (p - c));
    
}

